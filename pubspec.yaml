name: book_finder
description: A new Flutter project.

dependencies:
  flutter:
    sdk: flutter

  http: ^0.11.3+16

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true